# Devlog - 2026-04-07

## 1. Scope

- Close remaining deployment follow-up checks from P17/P20 handoff.
- Validate OpenWebUI optional adapter path with non-dry-run smoke.
- Validate Studio Ops token-enabled contract smoke in a live gateway profile.

## 2. Completed Work

### 2.1 OpenWebUI non-dry-run smoke (completed)

- Command:
  - `python scripts/open_webui_verify.py --skip-unit --run-smoke --no-dry-run --adapter-base-url http://127.0.0.1:8010 --api-key mini-agent-openwebui-token`
- Result:
  - `/health` passed (`guardrail_warning_count=0`)
  - `/v1/models` passed
  - `/v1/chat/completions` non-stream + stream passed
  - same `conversation_id` session continuity passed

### 2.2 Studio Ops token-enabled smoke (completed)

- Command:
  - `python scripts/studio_ops_smoke.py --base-url http://127.0.0.1:8008 --token studio-smoke-token --expect-auth --workspace-root C:/Users/Conli/Mini-Agent/src/workspace`
- Result:
  - auth boundary check passed (`401` without token)
  - provider CRUD/health contract path passed
  - memory summary/search/daily contract path passed
  - path boundary rejection checks passed

## 3. Documentation Sync

- Updated `docs/DEVELOPMENT_INDEX.md`:
  - `P17+` status updated to done
  - legacy deployment follow-up queue replaced with release/observation next queue
  - `最后更新` advanced to `2026-04-07`
- Updated `docs/DOCS_INDEX.md`:
  - switched latest devlog pointer to this file

## 4. Notes

- OpenWebUI remains optional adapter entry; primary product flow remains Studio + Gateway `/api/v1/*`.
- No new main-business dependency was introduced to OpenWebUI-only `/v1/*` endpoints.

## 5. Follow-up (test freeze + release gate stabilization)

### 5.1 Concurrency test freeze fixed

- Root cause:
  - `ConcurrencyManager.limited` was used as an async context manager but implemented as a plain async generator.
  - `RequestBatcher._process_batch` processed one batch and could leave queued requests unresolved.
- Changes:
  - `src/mini_agent/utils/concurrency.py`
    - added `@asynccontextmanager` to `ConcurrencyManager.limited`
    - reworked `_process_batch` to keep draining queued batches until empty
- Validation:
  - `pytest tests/test_concurrency.py -q` -> `11 passed`

### 5.2 Stable test runner and gate orchestration hardened

- `scripts/test_stable.py`
  - now runs pytest from `src` and targets `tests/` by absolute path to keep `mini_agent/config/*` path expectations stable.
- `scripts/release_gate.py`
  - added explicit local gateway stop before `stable_tests` step to avoid startup lock conflicts with TestClient-based gateway tests.
- Validation:
  - `python scripts/test_stable.py` -> `399 passed, 32 deselected`
  - `python scripts/release_gate.py --start-local-gateway --studio-token studio-smoke-token` -> `overall: PASS`
  - report: `workspace/release_gate/release_gate_20260406T195116Z.md`

## 6. OpenWebUI no-dry-run gate hardening (Windows)

### 6.1 Gateway runtime encoding fix

- Symptom:
  - OpenWebUI no-dry-run smoke intermittently failed with:
    - `gateway request failed (500): Agent execution failed: 'gbk' codec can't encode character ...`
- Root cause:
  - gateway-built `Agent` used default `console_output=True`, and runtime rich console output hit Windows GBK encoding constraints.
- Change:
  - `src/apps/agent_studio_gateway/main.py`
    - `_build_agent(...)` now sets `console_output=False`.

### 6.2 Full gate with OpenWebUI no-dry-run: PASS

- Execution profile:
  - started local OpenWebUI adapter (`127.0.0.1:8010`)
  - set adapter upstream timeout via `MINI_AGENT_OPENWEBUI_TIMEOUT_SECONDS=300`
  - ran:
    - `python scripts/release_gate.py --start-local-gateway --openwebui-run-smoke --openwebui-no-dry-run --openwebui-timeout 300 --openwebui-adapter-base-url http://127.0.0.1:8010 --openwebui-api-key mini-agent-openwebui-token --studio-token studio-smoke-token`
- Result:
  - OpenWebUI health/models/chat non-stream + stream + session continuity passed
  - Studio Ops smoke passed
  - stable tests passed (`399 passed, 32 deselected`)
  - report: `workspace/release_gate/release_gate_20260407T020305Z.md`

### 6.3 One-click local script for full gate

- Added:
  - `scripts/run_release_gate_openwebui.ps1`
- Script behavior:
  - auto-starts local OpenWebUI adapter when needed
  - exports required envs for adapter/gateway bridge
  - runs full gate with `--openwebui-run-smoke --openwebui-no-dry-run`
  - retries gate on transient failure (`MaxAttempts=2` by default)
  - stops adapter process if it was started by the script
- Validation:
  - `powershell -NoProfile -ExecutionPolicy Bypass -File scripts/run_release_gate_openwebui.ps1`
  - report: `workspace/release_gate/release_gate_20260407T023035Z.md` (`overall: PASS`)

## 7. P18 closeout + P19 prep + docs normalization

### 7.1 P18 closeout deterministic baseline frozen

- Re-ran deterministic gate (no external no-dry-run dependency in pass criteria):
  - `python scripts/release_gate.py --start-local-gateway --studio-token studio-smoke-token`
- Result:
  - `overall: PASS`
  - report: `workspace/release_gate/release_gate_20260407T031221Z.md`
- Baseline record:
  - added `docs/P18_CLOSEOUT_BASELINE_2026-04-07.md`

### 7.2 No-dry-run path volatility captured (advisory)

- Re-runs of one-click no-dry-run gate hit external-path instability and timed out:
  - reports: `workspace/release_gate/release_gate_20260407T025127Z.md`, `workspace/release_gate/release_gate_20260407T030059Z.md`
- Current policy in closeout docs:
  - deterministic gate is mandatory baseline
  - no-dry-run is advisory signal for external provider health

### 7.3 P19 prep contract drafted

- added `docs/P19_AGENT_TEAM_ROLLOUT_CONTRACT.md`
- contract scope:
  - keep `single_main` as default
  - make `team` mode opt-in with bounded concurrency and isolation constraints
  - define staged rollout + acceptance criteria

### 7.4 Docs/index normalization

- updated:
  - `docs/REFACTOR_TASKS.md` (P18 closeout/P19 prep status alignment + next queue refresh)
  - `docs/DEVELOPMENT_INDEX.md` (P18 done, P19 prep done marker)
  - `docs/DOCS_INDEX.md` (normalization addendum + historical-only scope note)
  - `docs/P18_HARD_REFACTOR_EXECUTION_PLAN.md` (closeout addendum linkouts)

## 8. P19 kickoff slice (runtime diagnostics + team guardrails)

### 8.1 Runtime diagnostics surfaced in system health

- Changes:
  - `src/mini_agent/runtime/main_agent_runtime_manager.py`
    - added `get_runtime_diagnostics()` snapshot method
    - diagnostics fields: `mode`, `active_sessions`, `max_active_sessions`, `available_session_slots`, `reserved_team_slots`, `workspace_application_required`, `main_workspace_dir`
  - `src/mini_agent/interfaces/system.py`
    - added `MainAgentRuntimeDiagnostics`
    - extended `SystemHealthResponse` with `runtime`
  - `src/apps/agent_studio_gateway/main.py`
    - `/api/v1/system/health` now returns runtime diagnostics payload

### 8.2 Team-mode bounded concurrency guardrail hardening

- Changes:
  - `src/mini_agent/runtime/main_agent_runtime_manager.py`
    - in `team` mode, requests without `session_id` now reuse existing workspace session (latest updated) instead of creating workspace-local duplicates
    - existing `max_active_sessions` hard rejection kept as 409 guardrail

### 8.3 Validation

- Targeted tests:
  - `pytest -q tests/test_main_agent_gateway_use_cases.py tests/test_agent_studio_gateway_api_v1.py`
  - result: `21 passed`
- P19 runtime matrix:
  - `python scripts/p19_runtime_matrix.py`
  - report: `workspace/p19_matrix/p19_runtime_matrix_20260407T050342Z.md` (`overall: PASS`)
- Stable baseline:
  - `python scripts/test_stable.py`
  - result: `410 passed, 32 deselected`
- Deterministic release gate recheck:
  - `python scripts/release_gate.py --start-local-gateway --studio-token studio-smoke-token`
  - report: `workspace/release_gate/release_gate_deterministic_20260407T050810Z.md` (`overall: PASS`)

### 8.4 Studio Ops runtime diagnostics exposure (Stage-B prep)

- Changes:
  - `src/apps/agent_studio_gateway/main.py`
    - added `GET /api/v1/ops/diagnostics/runtime` (Studio auth-protected)
  - `scripts/studio_ops_smoke.py`
    - added runtime diagnostics check (`[3/8] runtime diagnostics`)
  - `tests/test_agent_studio_gateway_studio_router.py`
    - added runtime diagnostics contract + auth coverage

### 8.5 Studio Ops diagnostics frontend view

- Changes:
  - `src/apps/agent_studio/src/types.ts`
  - `src/apps/agent_studio/src/api/ops.ts`
  - `src/apps/agent_studio/src/components/StudioOpsMode.tsx`
  - `src/apps/agent_studio/src/styles.css`
- Validation:
  - `pytest -q tests/test_agent_studio_gateway_studio_router.py tests/test_agent_studio_frontend_contract_client.py tests/test_agent_studio_gateway_api_v1.py`
  - result: `22 passed`

### 8.6 Release promotion checklist enforcement

- Changes:
  - `scripts/release_promotion_checklist.py`
    - executes deterministic gate as mandatory blocker
    - executes OpenWebUI no-dry-run gate as advisory signal
    - emits promotion report with `READY/BLOCKED` decision
  - `src/mini_agent/dev/release_promotion_checklist.py`
    - checklist policy evaluator + report renderer
  - `tests/test_release_promotion_checklist.py`
    - mandatory/advisory decision and report content coverage
- Validation:
  - `pytest -q tests/test_release_promotion_checklist.py`
  - result: `3 passed`
  - `python scripts/release_promotion_checklist.py --studio-token studio-smoke-token --skip-advisory`
  - report: `workspace/release_promotion/release_promotion_20260407T050810Z.md` (`Decision: READY`)

### 8.7 Stage-B guardrails diagnostics (queue saturation + workspace conflicts)

- Changes:
  - `src/mini_agent/runtime/main_agent_runtime_manager.py`
    - added team-mode counters:
      - `team_saturation_rejections`
      - `team_workspace_conflict_rejections`
  - `src/mini_agent/interfaces/system.py`
    - added the two counters to `MainAgentRuntimeDiagnostics` contract
  - `src/apps/agent_studio/src/types.ts`
  - `src/apps/agent_studio/src/components/StudioOpsMode.tsx`
    - Studio Ops runtime diagnostics panel now displays the two counters
  - `scripts/studio_ops_smoke.py`
    - runtime diagnostics smoke now validates the two counters are present and `>= 0`
- Validation:
  - `pytest -q tests/test_main_agent_gateway_use_cases.py tests/test_agent_studio_gateway_api_v1.py tests/test_agent_studio_gateway_studio_router.py tests/test_p19_runtime_matrix.py`
  - result: `31 passed`
  - `python scripts/release_gate.py --start-local-gateway --studio-token studio-smoke-token`
  - report: `workspace/release_gate/release_gate_deterministic_20260407T050810Z.md` (`overall: PASS`)

### 8.8 Operator runbook + CI release handoff + deterministic artifact gate

- Changes:
  - added runbook: `docs/P19_TEAM_MODE_OPERATOR_RUNBOOK.md`
    - `single_main` <-> `team` switch/rollback commands
    - operator validation recipe (matrix + gate + promotion checklist)
    - saturation/conflict counter interpretation
  - CI integration:
    - `.github/workflows/ci.yml`
    - new `release-handoff` workflow_dispatch job runs:
      - `python scripts/release_promotion_checklist.py --studio-token studio-release-handoff-token --skip-advisory`
      - `python scripts/check_deterministic_gate_artifact.py`
      - upload release handoff artifacts (`release_promotion_*.md`, `release_gate_deterministic_*.md`)
  - deterministic artifact guard:
    - `src/mini_agent/dev/deterministic_gate_artifact.py`
    - `scripts/check_deterministic_gate_artifact.py`
    - `tests/test_deterministic_gate_artifact.py`
- Validation:
  - `pytest -q tests/test_deterministic_gate_artifact.py tests/test_release_promotion_checklist.py`
  - result: `6 passed`
  - `python scripts/check_deterministic_gate_artifact.py`
  - result: `PASS`
  - negative path:
    - `python scripts/check_deterministic_gate_artifact.py --artifact-pattern workspace/release_gate/does_not_exist_*.md`
    - result: `FAIL` (missing artifact detected as blocking)

### 8.9 Stage-C external-facing docs (rollout announcement + support FAQ)

- Changes:
  - added rollout announcement:
    - `docs/P19_TEAM_MODE_ROLLOUT_ANNOUNCEMENT.md`
  - added support FAQ:
    - `docs/P19_TEAM_MODE_SUPPORT_FAQ.md`
- linked in index docs:
  - `docs/DEVELOPMENT_INDEX.md`
  - `docs/DOCS_INDEX.md`
  - `docs/REFACTOR_TASKS.md`
  - `docs/P19_AGENT_TEAM_ROLLOUT_CONTRACT.md`

### 8.10 Advisory CI automation + deterministic artifact guard closure

- `release-handoff` workflow updated to support optional advisory no-dry-run execution:
  - `workflow_dispatch` inputs:
    - `run_advisory_no_dry_run`
    - `advisory_adapter_base_url`
    - `advisory_timeout`
  - advisory path only runs when:
    - input is enabled, and
    - `OPENWEBUI_ADVISORY_API_KEY` secret is present
  - fallback behavior:
    - when advisory is requested but secret is missing, workflow emits note and runs deterministic-only checklist path
- deterministic artifact guard remains mandatory in CI:
  - step: `Validate Deterministic Gate Artifact (Required)`
  - script: `python scripts/check_deterministic_gate_artifact.py`
- YAML compatibility note:
  - `.github/workflows/ci.yml` uses quoted `'on'` key to avoid local YAML 1.1 parser ambiguity (`on` -> boolean) in offline sanity scripts.

Validation:
- `pytest -q tests/test_deterministic_gate_artifact.py tests/test_release_promotion_checklist.py` -> `6 passed`
- `python scripts/check_deterministic_gate_artifact.py` -> `PASS`
- `python scripts/release_promotion_checklist.py --studio-token studio-smoke-token --skip-advisory`
  - report: `workspace/release_promotion/release_promotion_20260407T055638Z.md` (`Decision: READY`)
  - deterministic gate report: `workspace/release_gate/release_gate_deterministic_20260407T055638Z.md` (`Overall: PASS`)
  - stable suite snapshot from gate: `413 passed, 32 deselected`

### 8.11 Team-mode ops alert policy definition and dashboard mapping

- Added policy doc:
  - `docs/P19_TEAM_MODE_ALERT_POLICY.md`
- Policy scope:
  - runtime diagnostics counters + capacity pressure ratio
  - signal thresholds for `healthy/watch/warning/critical`
  - operator action playbook per severity level
- Studio Ops UI mapping:
  - `src/apps/agent_studio/src/components/StudioOpsMode.tsx`
  - `src/apps/agent_studio/src/styles.css`
  - runtime diagnostics panel now renders:
    - overall alert status summary
    - per-signal severity chip/value/hint for capacity, saturation, workspace conflicts

### 8.12 Stage-C adoption tracking + canary cadence + weekly readiness reporting

- Added Stage-C adoption tracking doc:
  - `docs/P19_STAGEC_ADOPTION_TRACKING.md`
  - includes KPI definitions, weekly checklist, decision rule
- Added canary cadence doc:
  - `docs/P19_TEAM_MODE_CANARY_CADENCE.md`
  - defines daily matrix + deterministic gate checkpoints and weekly review routine
- Added weekly readiness template:
  - `docs/P19_WEEKLY_RELEASE_READINESS_TEMPLATE.md`
  - includes deterministic/advisory outcome snapshot and rollback decision log fields
- Added weekly rollout report generator:
  - `scripts/p19_weekly_rollout_review.py`
  - helper module: `src/mini_agent/dev/p19_rollout_reporting.py`
  - output: `workspace/p19_rollout/p19_weekly_rollout_<utc>.md`
- Tests:
  - `tests/test_p19_rollout_reporting.py`
  - `pytest -q tests/test_p19_rollout_reporting.py tests/test_deterministic_gate_artifact.py tests/test_release_promotion_checklist.py` -> `9 passed`
- Sample run:
  - `python scripts/p19_weekly_rollout_review.py --window-days 7`
  - report: `workspace/p19_rollout/p19_weekly_rollout_20260407T063932Z.md` (`Overall: READY`)

### 8.13 CI optional strict weekly review integration

- Updated workflow:
  - `.github/workflows/ci.yml`
- Added `workflow_dispatch` input:
  - `run_weekly_rollout_review_strict`
- Strict branch behavior:
  - runs matrix: `python scripts/p19_runtime_matrix.py`
  - runs strict weekly review: `python scripts/p19_weekly_rollout_review.py --window-days 7 --target-profile stage --strict --strict-targets`
  - uploads strict artifacts:
    - `workspace/p19_matrix/p19_runtime_matrix_*.md`
    - `workspace/p19_rollout/p19_weekly_rollout_*.md`
- Local strict verification:
  - `python scripts/p19_weekly_rollout_review.py --window-days 7 --target-profile stage --strict --strict-targets`
  - report: `workspace/p19_rollout/p19_weekly_rollout_20260407T064038Z.md` (`Overall: READY`)

### 8.14 Weekly aggregation hardening (runtime trends + target bands + previous-window diff)

- Added runtime snapshot artifact flow:
  - `scripts/studio_ops_smoke.py` now supports `--runtime-report-file`
  - `scripts/release_gate.py` auto-passes runtime snapshot output path to Studio Ops smoke
  - snapshot artifact example:
    - `workspace/release_gate/studio_ops_runtime_20260407T070735Z.json`
- Enhanced weekly rollout reporting:
  - `src/mini_agent/dev/p19_rollout_reporting.py`
  - additions:
    - runtime counter trend section (`saturation/conflict`: last/min/max/trend)
    - environment target bands (`dev/stage/prod`)
    - previous-window delta section
  - `scripts/p19_weekly_rollout_review.py` additions:
    - `--target-profile`
    - `--runtime-snapshot-pattern`
    - `--strict-targets`
- CI strict weekly path tightened:
  - `.github/workflows/ci.yml`
  - strict review command now:
    - `python scripts/p19_weekly_rollout_review.py --window-days 7 --target-profile stage --strict --strict-targets`

Validation:
- `pytest -q tests/test_p19_rollout_reporting.py` -> `4 passed`
- `pytest -q tests/test_release_promotion_checklist.py tests/test_deterministic_gate_artifact.py` -> `6 passed`
- `python scripts/release_gate.py --start-local-gateway --skip-openwebui-verify --skip-stable-tests --studio-token studio-smoke-token`
  - report: `workspace/release_gate/release_gate_20260407T070735Z.md` (`Overall: PASS`)
  - runtime snapshot: `workspace/release_gate/studio_ops_runtime_20260407T070735Z.json`
- `python scripts/p19_weekly_rollout_review.py --window-days 7 --target-profile stage --strict --strict-targets`
  - report: `workspace/p19_rollout/p19_weekly_rollout_20260407T070808Z.md`
  - `Overall: READY`, `Target Profile Status: PASS`

### 8.15 Mode-split/remediation/json summary follow-up

- Weekly report enhancements:
  - `src/mini_agent/dev/p19_rollout_reporting.py`
  - added:
    - `Runtime Mode Split` section
    - `Target Remediation Hints` section
    - machine-readable payload builder `build_weekly_rollout_payload(...)`
- Weekly script enhancements:
  - `scripts/p19_weekly_rollout_review.py`
  - added `--json-report-file` output
  - default JSON artifact path:
    - `workspace/p19_rollout/p19_weekly_rollout_<utc>.json`
- CI artifact upload updated:
  - `.github/workflows/ci.yml`
  - uploads `workspace/p19_rollout/p19_weekly_rollout_*.json`

Validation:
- `pytest -q tests/test_p19_rollout_reporting.py tests/test_release_promotion_checklist.py tests/test_deterministic_gate_artifact.py` -> `10 passed`
- `python scripts/p19_weekly_rollout_review.py --window-days 7 --target-profile stage --strict --strict-targets`
  - markdown: `workspace/p19_rollout/p19_weekly_rollout_20260407T072940Z.md`
  - json: `workspace/p19_rollout/p19_weekly_rollout_20260407T072940Z.json`
- `python scripts/p19_weekly_rollout_review.py --window-days 7 --target-profile prod`
  - report: `workspace/p19_rollout/p19_weekly_rollout_20260407T073001Z.md`
  - target profile status: `ATTENTION` (as expected with stricter prod band)

### 8.16 GitHub upload scope + cross-device handoff documentation

- Added upload range doc:
  - `docs/GITHUB_UPLOAD_SCOPE_2026-04-07.md`
  - includes:
    - recommended core upload scope
    - optional upload scope
    - excluded local artifacts
    - staging and pre-push safety commands
- Added cross-device handoff doc:
  - `docs/CROSS_DEVICE_HANDOFF_2026-04-07.md`
  - includes:
    - progress snapshot
    - latest evidence artifact pointers
    - bootstrap commands on new machine
    - minimal verification command set
    - next recommended focus queue
- Updated index docs:
  - `docs/DEVELOPMENT_INDEX.md`
  - `docs/DOCS_INDEX.md`
  - `docs/REFACTOR_TASKS.md`
- `.gitignore` hardened for local-only dev folders:
  - added `.claude/`, `.ruff_cache/`, `.pytest_cache/`, `.mypy_cache/`

### 8.17 Upload boundary hardening (secrets/runtime artifacts)

- Updated `.gitignore`:
  - ignore local secret files: `.env`, `.env.*` (keep `.env.example`)
  - ignore frontend cache: `*.tsbuildinfo`
  - ignore local runtime artifacts: `src/memory/*.md`, `src/smoke_test*`
- Added env template:
  - `src/apps/agent_studio_gateway/.env.example`
- Tightened upload docs:
  - `docs/GITHUB_UPLOAD_SCOPE_2026-04-07.md`
    - added explicit staged-file cleanup commands
    - added regex gate for blocked patterns (`.env`, `tsbuildinfo`, `src/memory`, `src/smoke_test`)
  - `docs/CROSS_DEVICE_HANDOFF_2026-04-07.md`
    - added new-device `.env` bootstrap steps from `.env.example`
    - added key rotation security reminder
